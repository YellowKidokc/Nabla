[project]
name = "canon-guard"
version = "0.1.0"
requires-python = ">=3.11"
description = "Version-aware canonical document drift gate"

[project.scripts]
canon-guard = "canon_guard:main"
equation-workflow = "equation_workflow:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
