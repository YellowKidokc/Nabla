# Sample paper using canonical definitions

This paper names [[def:terminus-sui]] explicitly. It also discusses the Gödel
incompleteness theorems. The variable G appears in an unrelated equation and
must remain unresolved rather than being silently linked to the grace factor.

G = m / r
