# 21_everyday_paradigm
> How does this change the way a person sees the problem?

**Branch:** public  ·  **Glyph:** `consciousness`

<!-- GENERATED FILE - do not hand-edit.
     Source: _vocab/stage_contracts*.json
     Regenerate: python _scripts/write_stage_contracts.py -->

## Permitted node types
- `paradigm`

## Required fields
- `ordinaryAssumption`
- `whatToSeeDifferently`
- `commonMistakeFromOldFrame`
- `whatBecomesVisible`

## Allowed incoming edges
- `descendsTo <- 11_technical_paradigm`

## Allowed outgoing edges
- `feedsInto -> 26_audience`

## Completion condition
A named ordinary mistake that follows from the old frame.

## Propagation behavior
Local.

## NLP / classifier operations
Detect simplified-literature-review drift. This is a lived mental-model change, not a summary.

## Validation rules
Asks what a PERSON sees differently, not what a DISCIPLINE changes. Those are separate nodes.

## Public rendering
Reframe section.

---

## Folder state
The icon on this folder reflects its state automatically:
`gray` empty · `amber` working · `green` done (`_STATUS.done`) · `red` failed (`_STATUS.failed`)

Run `python _scripts/set_folder_icons.py` after changing content.
